# Data directories (contain runtime data)
data/

# Downloaded binaries
contrib/

# Environment files with secrets
.env

# Secret-export scripts (template + any filled-in copy)
set-env-template.sh
set-env*.sh

# Python
__pycache__/
*.py[cod]
.venv/
uv.lock

# AWS credentials and keys
aws_credentials.yaml
*.pem

# IDE
.idea/
.vscode/
*.swp
config/ssh/
